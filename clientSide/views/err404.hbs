{{>header}}
{{>navbar}}
<section class="container my-5">
    <div class="alert alert-danger">
        <h2>{{message}} {{looking}}</h2>
        <p>{{err}}</p>
        <a href="/" class="btn btn-primary">Back Home</a>
    </div>
</section>
{{>footer}}